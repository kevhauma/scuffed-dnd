# Custom DnD Builder — v2.0 Sheet Core (Overview)

The second milestone: rework the app's core model so it matches what the source spreadsheet
actually does, as reverse-engineered in [`docs/excel export summary/`](../excel%20export%20summary/ttrpg-app-spec.md).
v1.0 was built from a working memory of the sheet; the export summary (2026-08-03) documented the
real mechanics cell-by-cell and showed the core missed in four structural places — the stat/skill
split, the skill level/bonus machinery, the specialisation mechanism, and the progression loop.
This milestone corrects the core. Content concepts built *on* that core (creatures, spells,
passives, harvest tables, item templates, material generators, currency rework) are later
milestones.

**Requirement source for this version:** the concept pages under
[`docs/excel export summary/concepts/`](../excel%20export%20summary/concepts/README.md), plus the
[spec](../excel%20export%20summary/ttrpg-app-spec.md). Tickets cite concept page numbers in their
`Traceability` line (e.g. `Concept 01 · Stat`); v1.0 requirement numbers appear only where a v1.0
behaviour is deliberately kept. There is no separate `requirements.md` here — the concept pages
are the requirements.

## Decisions (2026-08-03)

Made by the User when this milestone was scoped. Settled — don't re-open without a new decision here.

- **Option (a): correct the core, then build outward.** The current model is not "our own system";
  the goal is the sheet.
- **The product concept is unchanged: browser-only, no backend, one configuration.** Everything in
  the spec that assumes a server (multi-user roles, draft/publish versioning, audit logs) is out of
  scope for this milestone and probably forever. "Just a config."
- **Everything is configurable** — every name, abbreviation, and formula is data the User can edit,
  and renaming must never break a reference (Concept 00 §6). That is TICKET-REF-01 plus the entity
  tickets.
- **Clean break on persisted data.** The v2 `Configuration`/`Character` shapes are not compatible
  with v1. v1 LocalStorage and v1 JSON exports are rejected with a clear notice, not converted —
  old characters (focus stat, spend-derived level, speciality base levels) have no faithful mapping
  into the new model. TICKET-IO-03.
- **Tickets stay small.** At most **three to-be items** per ticket; anything larger splits into
  sequential tickets (User request, 2026-08-03). Engine/UI splits follow the ROLL-01/ROLL-02
  precedent. Apply this to every future ticket in every version.
- **The v1 skill triad collapses into the sheet's concepts:**

  | v1 concept | becomes | in tickets |
  |---|---|---|
  | `MainSkill` + `Stat` | one **Stat** concept (invested *or* derived, flags for resource/total) | STAT-01 · STAT-02 · STAT-03 |
  | `SpecialitySkill` | **Skill** — weighted stat derivation with level *and* bonus | SKL-02 · SKL-03 |
  | `CombatSkill` | **Roll definition** — input expression fed through a dice ladder | ROLL-05 · ROLL-06 |
  | focus stat (`focusStatCode` / `focusStatBonusLevel`) | **retired** — replaced by Archetype | ARC-03 |
  | `mainSkillPointBudget` (flat pool) | budget derived: `level × const.points_per_level` | RES-02 |

## Not in this milestone (deliberately)

Creature/bestiary, Spell, Passive, Harvest table, Creature type, Size, Damage & check types,
item template/instance split, slot `count`/`accepts`, material tier **generators**, currency
base-unit rework, the xlsx importer, Views beyond the existing sheet, versioning/permissions.
Each needs the corrected core first; ticket them in a later version once this one lands.

## Open — in build order

Formula engine (all additive, suite stays green):

- [x] [TICKET-FORM-02](./tickets/TICKET-FORM-02-formula-function-library.md) — Function calls in the formula engine (Concepts 01, 02) — **first**: `round`/`max` unlock every confirmed derivation
- [x] [TICKET-FORM-03](./tickets/TICKET-FORM-03-namespaced-reference-syntax-and-resolution.md) — Namespaced reference syntax and resolution (Concept 00 §5) — needs FORM-02's parser work fresh
- [x] [TICKET-FORM-04](./tickets/TICKET-FORM-04-namespace-scoping-and-cycle-detection.md) — Namespace scoping table + cycle detection across namespaces (Concept 00 §5) — pairs with FORM-03
- [x] [TICKET-FORM-05](./tickets/TICKET-FORM-05-errors-as-values-engine.md) — Errors as values in the engine (Concept 00 §7) — before the entity tickets multiply `CalculatedCharacter` consumers
- [x] [TICKET-FORM-06](./tickets/TICKET-FORM-06-error-chips-on-the-sheet.md) — Error chips on the character sheet (Concept 00 §7) — renders FORM-05; closes the v1.0 known bug's visible half
- [x] [TICKET-CALC-02](./tickets/TICKET-CALC-02-seed-configured-main-skills.md) — An unallocated main skill is 0, not missing (bug fix, Concept 01, v1.0 Req 16.6 — `calculateTotalMainSkillLevels` seeds from `character.mainSkillLevels`, so a configured-but-unallocated code is absent from the formula context and every formula naming it reports `Undefined variable`) — **directly after FORM-06**: that ticket made the v1.0 known bug survivable, this removes its cause; a few lines on the current model, and the invariant carries into STAT-01

Identity and the two new config concepts:

- [x] [TICKET-REF-01](./tickets/TICKET-REF-01-stable-ids-and-rename-safety.md) — Stable ids and rename-safe formulas (Concept 00 §6) — the "every name configurable" decision; before the entity tickets *(browser criterion left open — the User skipped the live check for this run)*
- [x] [TICKET-REF-02](./tickets/TICKET-REF-02-guarded-deletes.md) — Guarded deletes with reference lists (Concept 00 §6) — needs REF-01's references and FORM-05's error values *(browser criterion left open — the User skipped the live check for this run)*
- [x] [TICKET-CST-01](./tickets/TICKET-CST-01-constants-concept.md) — Constants entity and `const.*` (Concept 05) — SKL-02, RACE-02, RES-02, ARC-02 read these
- [x] [TICKET-CST-02](./tickets/TICKET-CST-02-constants-panel.md) — Constants panel (Concept 05) — UI for CST-01; closes CST-01's implementation note 4, the identifier rule now enforced for User input as well as for import *(browser criterion left open — the User skipped the live check for this run)*
- [x] [TICKET-CRV-01](./tickets/TICKET-CRV-01-curve-entity-and-lookup.md) — Curve entity and lookup engine (Concept 06) — RES-01 (reverse) and ARC-02 (multi-column) consume it. **Carries a syntax divergence**: the multi-column call is `curve.name.column(x)`, not the to-be's `curve.name(x, column)`, which the grammar cannot express — see the ticket's implementation note 1. Pure engine, so no browser criterion
- [x] [TICKET-CRV-02](./tickets/TICKET-CRV-02-curve-generators-and-overrides.md) — Curve generators with preserved overrides (Concepts 06, 00 §1.1) — the generate-and-overlay machinery. Pure engine, so no browser criterion. **Raised [TICKET-FORM-07](./tickets/TICKET-FORM-07-exponentiation.md)**: Concept 06's own XP generator cannot be written without exponentiation
- [x] [TICKET-CRV-03](./tickets/TICKET-CRV-03-curves-panel-and-seeds.md) — Curves panel and the `point_buy` / `xp_thresholds` seeds (Concept 06) — UI + seeds for CRV-01/02. **Closed CRV-01's open column question the hard way**: a column name is now id-resolved through a curve-scoped space in `references.ts`, so renaming one re-spells every formula, and removing one is a guarded delete like any other entity *(browser criterion left open — the User skipped the live check for this run)*
- [x] [TICKET-FORM-07](./tickets/TICKET-FORM-07-exponentiation.md) — Exponentiation in the formula engine (Concept 06, spec §5.3) — raised by TICKET-CRV-02, which found that Concept 06's own XP generator cannot be written. **Decided by the User: the `^` operator.** Right-associative (where Excel would disagree — a deliberate split recorded in the ticket) with unary minus binding tighter, as in Excel. Also closed a wider hole it exposed: no arithmetic result can now leave the evaluator as `NaN` or `Infinity`, and the typecheck baseline fell 4 → 2. Pure engine, so no browser criterion

The schema rework:

- [x] [TICKET-STAT-01](./tickets/TICKET-STAT-01-unified-stat-model-and-engine.md) — The unified Stat model and engine (Concepts 01, 00) — **the centrepiece**; merges `MainSkill` + `Stat`, new character shape, composition calculator. The flat formula space now holds **stat abbreviations**, which is what let `MainSkill` be deleted without redesigning races, materials and both skill kinds in the same ticket; `Configuration.schemaVersion: 2` is in and `storage.ts` refuses v1 by name, leaving IO-03 the notice rather than the detection
- [x] [TICKET-IO-03](./tickets/TICKET-IO-03-v2-shape-clean-break.md) — Clean break: reject v1 data with a clear notice (decision above) — immediately after STAT-01 so nothing misloads old data. The refusal is a *refusal*: `IncompatibleDataNotice` renders **instead of** the routes, so nothing downstream can mint a fresh ruleset and save it over data the User has not agreed to lose, and only their confirmed start-fresh reaches `clearAllData`. An imported v1 file gets its own `SchemaVersionError` rather than a field-by-field report. **Raised one out-of-scope gap** — a v1 *characters* key beside an absent or v2 config is still dropped silently; see the ticket's implementation note 5 *(browser criterion left open — the User skipped the live check for this run)*
- [x] [TICKET-STAT-02](./tickets/TICKET-STAT-02-unified-stats-panel.md) — Unified stats configuration panel (Concept 01) — the real editor replacing STAT-01's mechanical patches. Every field now edits in one place, and `reorderStats` writes the array and `order` from one sequence so the User's ordering drives display everywhere without each reader remembering to sort. **One divergence from the to-be**: a duplicate abbreviation stays a *refusal* rather than becoming a warning — the flat formula space cannot hold two `STR`s — see the ticket's implementation note 1; the *resource-without-a-ceiling* rule is the genuine warning. Also restored `StatsConfigPanel.test.tsx`, one of the five panel tests TICKET-DX-01 deleted *(browser criterion left open — the User skipped the live check for this run)*
- [x] [TICKET-FORM-08](./tickets/TICKET-FORM-08-formula-preview-with-substitutable-variables.md) — Formula preview with substitutable variables (Req 3.1–3.3, 16.4) — **directly after STAT-02** so the preview lands in the real stats editor rather than in STAT-01's interim patch, and the stat card's one-off preview retires with it. `FormulaPreview` lives in `components/config/shared/`: editable sample boxes plus the fixed 1–50 ladder, every number through `evaluateFormulaString` at the owner's real scope. `StatCard` now evaluates nothing at all. **Note for FORM-09**: the component takes a `Configuration`, not the `NamespaceSource` this ticket's notes anticipated — `scopeFor` needs the whole ruleset — see implementation note 1 *(browser criterion left open — the User skipped the live check for this run)*
- [x] [TICKET-FORM-09](./tickets/TICKET-FORM-09-formula-preview-everywhere.md) — Every formula field previews (Req 4.3, 4.5, 5.4, 16.1–16.4, 16.6) — needs FORM-08; placements only. SKL-03 later reworks the speciality dialog and ROLL-05 replaces the combat one — both keep the preview under the standing rule in [CLAUDE.md](../../CLAUDE.md). All four formula fields preview now, each at its own `FormulaOwner`, and the curve-generator ladder sweeps `key` so a generator can be read before it overwrites a table. **Two changes to FORM-08's component, recorded there**: an `unknown-namespace`/`unknown-member` result collapses to one explanatory line rather than nine dashes, and the sample boxes swallow Enter so typing in one no longer submits the dialog *(browser criterion left open — the User skipped the live check for this run)*
- [x] [TICKET-STAT-03](./tickets/TICKET-STAT-03-wizard-and-sheet-on-unified-stats.md) — Wizard and sheet on unified stats (Concept 01) — play surfaces; resource gating of current values. The sheet is now **one** grid: every stat gets the same breakdown row, and a resource gets the `StatEditor` *as well* rather than instead, so "what is this stat" and "where does the pool stand" stopped being the same question. `DerivedValue` moved to [`components/play/derivedValue.ts`](../../src/components/play/derivedValue.ts) so the wizard's derived preview reads the engine's errors the same way the sheet does. **One divergence**: a resource whose formula is broken chipped twice, once per row — `StatEditor` now states `maximum unavailable` in words and leaves the chip to the breakdown row, see implementation note 1

Mechanics on the new core:

- [x] [TICKET-RACE-01](./tickets/TICKET-RACE-01-race-stat-blocks.md) — Races as stat blocks (Concept 04 subset) — shape + editor. `Race.statValues` is keyed by **stat id**, so unlike `skillModifiers` it needs no display↔stored translation at all and the race branch left `references.ts` entirely. **Raised a decision the rest of the milestone inherits**: `SUPPORTED_SCHEMA_VERSION` is bumped on *every* reshape from here (2 → 3 here), so stale in-milestone data meets IO-03's notice instead of crashing on a field that moved — see implementation note 1. Left for RACE-02: whether a *derived* stat should get a row in the block editor at all (note 5)
- [x] [TICKET-RACE-02](./tickets/TICKET-RACE-02-blended-bases-and-cardinality.md) — Blended bases, 1–2 races (Concepts 04, 05) — wires the base term; fixes additive stacking. **Cardinality shipped as an upper bound**: a third race is refused, zero stays legal, since a ruleset may define no races (implementation note 1). Two smaller decisions the rest of the milestone inherits: `roundup` outside the evaluator now means Excel's away-from-zero via a shared `roundAwayFromZero`, and the engine reads `const.race_blend_divisor` **by name** — the first constant it reads at all — which means renaming that constant falls back to the seeded 2 rather than re-spelling (notes 2 and 3) *(browser criterion left open — the User skipped the live check for this run)*
- [x] [TICKET-MAT-01](./tickets/TICKET-MAT-01-modifiers-target-stats.md) — Material modifiers target stats (Concept 09) — shape + dialog. `MaterialLevel.bonuses` is `StatModifier[]` keyed by stat **id**, which retires the last persisted use of `skillCode` outside `CombatSkill`: `references.ts` now translates formula strings and nothing else. **Materials can no longer buff a speciality or combat skill** — the shape has no way to say it, which is Concept 09's model; a skill moves with equipment through the stats its formula reads (implementation note 1). The equipment *aggregate* is deliberately unchanged — `calculateEquipmentBonuses` spells each `statId` as its abbreviation on the way out — so MAT-02 has exactly one bridge to remove. `SUPPORTED_SCHEMA_VERSION` 3 → 4. One divergence: a derived stat is **omitted** from the picker rather than offered and refused, with the refusal kept where it must be, in the validator (note 2)
- [x] [TICKET-MAT-02](./tickets/TICKET-MAT-02-equipment-bonuses-on-stats.md) — Equipment bonuses land on stats (Concepts 01, 09) — completes the composition; "+50 Mana" works. The aggregate is `StatModifier[]` keyed by stat id end to end, so **STAT-01's equipment bridge is gone and `SkillModifier` is deleted outright**. The speciality and combat calculators lost their equipment parameters entirely — equipment applies exactly once, at the stat composition, which makes Req 13.2 structural rather than a property of the code spaces staying disjoint, and closed a latent double-count on a stat/skill code collision. **Requirement 6.7 now has no implementation anywhere** (MAT-01 removed the shape, this the term); the citation is dropped rather than left claiming it, and the sheet's speciality rows lose their `equipment` contribution with it *(browser criterion left open — the User skipped the live check for this run)*
- [x] [TICKET-SKL-02](./tickets/TICKET-SKL-02-weighted-skills-level-and-bonus.md) — Skill entity and weighted derivation (Concept 02) — level + `round(level / const.bonus_divider)`. **The source landed a session ahead of its tests**: three ad-hoc commits migrated `src/` and left the whole test layer on `SpecialitySkill` — 171 failures and 83 type errors from one cause, invisible to the pre-commit hook because Biome was clean. Finishing the ticket was mostly the test layer catching up, and it surfaced three real defects on the way: `levelOf`'s `withSource` could never fire, so a skill was never named in its own error chain; `FormulaPreview` could not express `skills.*` at all (extended per the standing rule, recorded on FORM-08); and `focusStatCode` is now the one code-keyed character field with nothing chasing a rename — left for ARC-03, which retires the focus stat, and recorded in three places rather than fixed. Also: FORM-09's four preview placements are three, a `Skill` having no formula field. The `conventions-reviewer` then caught the preview change half-done — a `skills.*` reference got no sample box, so it previewed a confident `0`; fixed, along with four smaller findings, in the last commit *(browser criterion left open — the User skipped the live check for this run)*
- [x] [TICKET-SKL-03](./tickets/TICKET-SKL-03-skills-panel-and-sheet-grid.md) — Skills panel, sheet grid, skill validation (Concept 02) — UI + the three validation rules. **The as-is was overstated and is corrected on the ticket**: SKL-02 had already replaced the panel's formula field with weight rows, so the first to-be item was *untested* rather than unbuilt — and testing it found a real defect, an emptied weight box persisting `NaN` and poisoning every level the skill fed. The sheet's row now carries both of Concept 02's numbers, the level in `SkillBreakdownRow`'s new `secondary` slot beside the bonus, over a breakdown the **calculator** multiplies out (`skillContributions`) rather than the component. **Raised a third severity the rest of the milestone inherits**: `information`, for an observation that is not a defect — the weight-sum balance rule is the first, and reporting it as a warning would have trained the User to ignore warnings; it never affects `isValid`, and a consumer flattening a report must now include all three arrays *(browser criterion left open — the User skipped the live check for this run)*
- [x] [TICKET-RES-01](./tickets/TICKET-RES-01-xp-and-derived-level.md) — XP and curve-derived level (Concepts 20, 06) — inverts v1.0's spend-derived level. `Character.experience` is the **second** sanctioned piece of stored player state beside `currentResourceValues`, and `calculateCharacterLevel` now reads it backwards out of the `xp_thresholds` curve rather than summing invested points, so the chain finally runs `XP → level → budget → spend` (RES-02 closes the budget half). **Level became a `FormulaResult`** at the User's decision: the curve is User data that can be deleted or set to refuse out-of-range input, and a confident level 1 there would silently misprice every budget RES-02 derives — so the sheet header and the character card chip it, and `CharacterSummary.level` widened with it. Widening `calculateCharacterLevel` to take the `Configuration` turned all three call sites into compile errors rather than stale reads. `SUPPORTED_SCHEMA_VERSION` 5 → 6, which rippled to 37 test fixtures plus `scripts/build-sheet-import.mjs` and `examples/demo-ruleset.json` — **the last two caught by their own guards rather than by anyone noticing**, which is what `sheetImport.test.ts` and `exampleRuleset.test.ts` were written to do *(browser criterion left open — the User skipped the live check for this run)*
- [x] [TICKET-RES-02](./tickets/TICKET-RES-02-level-derived-point-budget.md) — Level-derived point budget (Concepts 06, 05) — retires the flat pool; needs RES-01. Closes the chain RES-01 opened: `XP → level → budget → spend` now runs end to end, and `Configuration.mainSkillPointBudget` is deleted with its "absent means unlimited" — there is no longer any way to express an unlimited pool. **The budget is a `FormulaResult`**, inheriting RES-01's reasoning: a level that cannot be read makes the allocation *invalid* rather than unlimited, because a confident budget would let a Player spend points the ruleset never granted. **Two decisions the rest of the milestone inherits**: retiring a persisted field is now a **refusal** rather than a silent drop (`RETIRED_FIELDS` in `importExport.ts` names what replaced each one), and the to-be's "wizard allocation UI reachable post-creation" turned out not to exist as a pattern — so the spend surface is an `InvestedPointsEditor` row on the sheet, given to an invested stat exactly the way STAT-03 gave a resource the `StatEditor`. `SUPPORTED_SCHEMA_VERSION` 6 → 7 *(browser criterion left open — the User declined the live check for this run and the rest of the milestone)*
- [x] [TICKET-RES-03](./tickets/TICKET-RES-03-resource-pool-behaviours.md) — Resource pool behaviours (Concept 20) — reset-to-max, relative entry, kept-and-flagged. **One spelling had to pick a meaning**: Concept 20's `-7` quick entry collides with v1.0's typeable negative absolute, and relative won — a table deals in damage and healing, and a pool still reaches −10 by taking 70 off 60, so Requirement 14.4 is intact and only the keying moved. The kept-and-flagged rule is the substantive one: a fallen maximum leaves the stored current alone and marks it, because "a derived max must never silently overwrite what the player is tracking" — writes still clamp, so the state resolves the moment the Player touches the pool. Also **carries the eight `conventions-reviewer` findings from RES-02**, recorded on that ticket: the largest was a live bug where typing a multi-digit unaffordable number quietly unspent points, fixed by the same commit-on-blur rewrite this ticket needed *(browser criterion left open — the User declined the live check for this run and the rest of the milestone)*
- [x] [TICKET-ARC-01](./tickets/TICKET-ARC-01-archetype-concept-and-panel.md) — Archetype concept and panel (Concept 03) — entity + affinity editor. The eleventh config panel, at `/config/archetypes`. **`non` is absence, not a value**: a tagging is stored sparsely, so a stored `non` cannot read as a reference and make `deleteStat` refuse for every stat every archetype was ever saved over — the trap RACE-01 avoided by pruning zeros, met again and settled the same way. **Refines RACE-01's schema-bump rule rather than following it**: nothing moves here, so a purely additive optional field (`archetypes?`, `Character.archetypeId?`) gets no bump — the rule is about a build crashing on a field that *moved*. `Character.archetypeId?` lands early, in this ticket rather than ARC-03, because without something that can hold an archetype the guarded-delete criterion would be an unfalsifiable green box. The sheet fragment ships `confidence: partial` and tags only each archetype's **main** stat, because that is all the export proves — the sub/non split is Concept 03's open question and is left to trip the default-to-non warning rather than invented *(browser criterion left open — the User declined the live check for this run and the rest of the milestone)*
- [x] [TICKET-ARC-02](./tickets/TICKET-ARC-02-curve-routed-stat-gains.md) — Curve-routed stat gains (Concepts 03, 06) — the exchange rates; needs ARC-01, CRV-03, RES-02. STAT-01's provisional 1:1 term is gone: 15 points now buy **12 / 7 / 5** by affinity against the seed table, the 2.4× spread the sheet confirms, and **`investedStatPoints` means points *spent* rather than levels gained** — the curve is the exchange rate between the two. **Three rules the table cannot state** are decided in code and recorded on the ticket: spending nothing gains nothing (the seed's `main` generator reads 0.75 at zero, being fitted over the range a Player spends in); no `point_buy` curve falls back to 1:1 so every pre-ARC-02 ruleset keeps working; and a spend past the table's last row **chips** rather than falling back, because 1:1 there would hand a Player more than the main column ever grants. `investedValue` returning a `FormulaResult` is what makes that last one expressible. **Not the pure-engine ticket it was written as**: the `conventions-reviewer` found the sheet's breakdown had stopped adding up — `invested +15` beside a total of 14 — and that the suite could not see it, because no fixture carried a `point_buy` curve while every *real* ruleset seeds one. The breakdown now reads `invested 15 → +12`, the wizard reads the engine's gains instead of adding points to a race base itself, and a spend the table cannot price is **refused** rather than saved and chipped later *(browser criterion added by that finding and left open — the User declined the live check for this run and the rest of the milestone)*
- [x] [TICKET-ARC-03](./tickets/TICKET-ARC-03-wizard-step-and-focus-stat-retirement.md) — Wizard archetype step; retire the focus stat (Concept 03) — closes the character-build loop, and the triad-collapse row *focus stat → retired* with it. The step sits **before** allocation rather than where the focus step was: the archetype decides what a point is worth, so a Player has to know that before choosing where to spend. **Two divergences, both recorded**: `Character.archetypeId` stays *optional* — the wizard requires a pick only when the ruleset defines archetypes, because a ruleset may define none, exactly as RACE-02 kept a raceless character legal; and `focusStat` is not literally zero hits in `src/`, because three references *are the retirement record* (the `RETIRED_FIELDS` entry naming the replacement, the test asserting absence, the schema history). **The rename problem is now closed rather than managed**: the focus stat was the last character field keyed by a code, so `combatSkillReferences` no longer takes characters at all. `SUPPORTED_SCHEMA_VERSION` 7 → 8 *(browser criterion left open — the User declined the live check for this run and the rest of the milestone)*

Rolls, then the gate:

- [x] [TICKET-ROLL-03](./tickets/TICKET-ROLL-03-dice-ladder-engine.md) — Dice ladder entity and decomposition (Concept 07) — pure engine. `decomposeValue` walks a ladder greedily and **conserves its input**, which is the property the whole thing is tested on: a negative value, a fractional one, a rungless ladder and a nonsensical cap all come back flat-only rather than throwing, so `flat + Σ(size × count)` is the input in every case and the validator is what reports the ruleset problem. All six of Concept 07's ✅ rows are pinned, twice — once on a fixture and once end to end on the merged corpus. **The delete is deliberately unguarded**, the one entity here whose delete is: nothing can point at a ladder until ROLL-05's roll definitions, and a `ReferenceTargetKind` with no possible referrer is the unfalsifiable green box ARC-01 avoided by landing `Character.archetypeId?` early — so ROLL-05 adds the guard alongside the thing that makes it falsifiable. **No schema bump**: `diceLadders?` is purely additive, so it takes ARC-01's refinement of RACE-01's rule rather than the rule itself. The `conventions-reviewer` then found the one place the walk was **not** total — a `NaN` die size poisoned every count and the flat, breaking the invariant the module's own docblock states — which is the shape of defect a conserves-its-input property test is written to catch and did not, because `fast-check` was generating values rather than ladders *(browser criterion: none — pure engine and types, nothing UI-visible)*
- [x] [TICKET-ROLL-04](./tickets/TICKET-ROLL-04-ladder-rolling-and-notation.md) — Ladder rolling and notation (Concept 07) — pure engine; pairs with ROLL-03. `rollDecomposition` takes a **decomposition** rather than a value and a ladder, which is `rollCombatSkill`'s rule applied here: the same object is shown and rolled, so a roll cannot disagree with the sheet. `formatLadderNotation` is the single notation definition for ladder pools and is the opposite of `formatDiceNotation` on every count — descending not ascending, uppercase `D`, zero rungs kept per `showZeroTerms`, flat always rendered — which is why they are two functions rather than one with a flag; ROLL-06 deletes the older one. **It never sorts**: a misordered ladder is the validator's error to report, and sorting here would be a second opinion that disagrees with the report. The property test sweeps **generated ladders**, closing the gap that let ROLL-03's `NaN`-size defect past its own properties *(browser criterion: none — pure engine, nothing UI-visible)*
- [x] [TICKET-ROLL-05](./tickets/TICKET-ROLL-05-roll-definitions-and-panel.md) — Roll definitions and the rolls panel (Concept 08) — entity + editor + seeds. The eleventh route, at `/config/rolls`, mounting **two** panels — a roll and the ladder it decomposes down are separate entities, so each owns its own `ConfigPanelShell`, the shape `/config/items` uses. **The first cut was one panel with two managers, and the `conventions-reviewer` refused it**: the second entity had to hand-write the header the shell owns, at a different heading level than the shell emits, which is exactly the drift DX-05 existed to remove — reintroduced one ticket after it landed. `deleteDiceLadder` is guarded now, closing what ROLL-03 left open deliberately; `roll-definition` is a reference kind whose case **returns nothing on purpose** — no formula can name a roll, since a roll produces dice rather than a number, and history is session state, so a roll is a leaf and the case says so rather than being forgotten. **One divergence from the to-be, and it is the interesting one**: the seeded inputs are `0`, not `stats.str`. A fresh ruleset has no stats, so the to-be's four expressions would name members that do not exist and a brand-new configuration would open reporting four errors — the seed states the *shape* (four rolls, one ladder) and each description says what the sheet reads there. The real expressions live in the sheet corpus, where the stats exist; **evasion and endure ship 7 and 4 short**, because the sheet's extra term is Concept 08's open question and a sourced gap beats an invented constant *(browser criterion left open — the User declined the live check for this run and the rest of the milestone)*
- [ ] [TICKET-ROLL-06](./tickets/TICKET-ROLL-06-sheet-rolls-and-combat-skill-removal.md) — Sheet rolls the definitions; `CombatSkill` removed (Concept 08) — completes the triad collapse
- [x] [TICKET-DX-05](./tickets/TICKET-DX-05-shared-config-panel-shell.md) — One shell for every configuration panel (Req 21.4, 21.5, 21.7) — taken **early rather than second-to-last**, at the User's request, after RACE-01's one-line header edit made `fallow audit` attribute three introduced clone groups to it. `ConfigPanelShell` in `config/shared/` is the frame for all **eleven** config components now, `BaseSkillPanel` included — it is the skills *specialisation* of the shell rather than a second frame, which settles the h3/h4 divergence at **h4**. Two divergences from the to-be, both recorded on the ticket: the no-ruleset guard stays a one-line early return rendering a shared `NoConfigurationNotice` (children are evaluated at the call site, and the return is what narrows `config` for TypeScript), and the empty state is a sibling `ConfigEmptyState` rather than a shell prop (a section can have more than one list)
- [ ] [TICKET-DX-04](./tickets/TICKET-DX-04-golden-fixtures-from-the-sheet.md) — Golden fixtures from the sheet (spec §12) — **last**: the milestone's parity gate and final checkpoint

## Definition of Done (applies to every ticket)

Per [../../CLAUDE.md](../../CLAUDE.md): `npx vitest run` green — 0 failures, 0 skips —
`npx tsc --noEmit` with no errors beyond the documented baseline in
[TEST_STATUS.md](../../TEST_STATUS.md), `yarn run check` clean (the pre-commit hook enforces it),
verification via the `verifier` subagent plus the `fallow` and `coding-conventions` skills, and a
live browser check for anything UI-visible (ask the User first; if declined, the criterion stays
open with a note). A ticket that adds or reshapes a persisted entity also lands that feature's
fragment in [`docs/imports/`](../imports/README.md) — the real sheet data, cell ranges cited — and
reruns `yarn run sheet:import`. Persistence goes through a store action. Derived values come from the engine
and are never persisted onto `Character` — the sanctioned stored player state after this
milestone is exactly resource current values, invested points, and accumulated XP. Feature
components compose `components/ui` primitives; theme tokens only; `src/routeTree.gen.ts` is never
hand-edited.
