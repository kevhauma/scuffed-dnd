/**
 * Class strings for the provider buttons (TICKET-AUTH-02)
 *
 * The divider is the only thing here with any thought in it: *or* between the password form and the
 * provider buttons needs a rule either side of a word, and `Divider` is a rule without one.
 */

/** The `or` rule — a hairline either side of a word, on parchment */
export const separatorStyles = 'flex items-center gap-3 text-ink-600';

/** One half of that hairline */
export const separatorRuleStyles = 'h-px grow bg-brass-dark/40';
